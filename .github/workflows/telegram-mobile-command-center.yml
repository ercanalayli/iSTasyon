name: AperiON Telegram Mobile Command Center

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: aperion-telegram-mobile-command-center
  cancel-in-progress: false

jobs:
  install:
    name: Install secure mobile command center
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Verify required secrets
        shell: bash
        run: |
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"
          test -n "$TELEGRAM_BOT_TOKEN"

      - name: Install pinned Wrangler
        shell: bash
        run: npm install --no-save --ignore-scripts wrangler@4.50.0

      - name: Apply D1 migrations
        shell: bash
        run: npx wrangler d1 migrations apply aperion-control-plane --remote

      - name: Resolve authorized Telegram chat
        shell: bash
        run: |
          result="$(npx wrangler d1 execute aperion-control-plane --remote --json --command \
            "SELECT chat_id, COUNT(*) AS message_count FROM quick_notes WHERE chat_id IS NOT NULL AND chat_id <> '' GROUP BY chat_id ORDER BY message_count DESC LIMIT 1")"
          TELEGRAM_CHAT_ID="$(printf '%s' "$result" | jq -r '.[0].results[0].chat_id // empty')"
          test -n "$TELEGRAM_CHAT_ID"
          echo "::add-mask::$TELEGRAM_CHAT_ID"
          echo "TELEGRAM_CHAT_ID=$TELEGRAM_CHAT_ID" >> "$GITHUB_ENV"

      - name: Prepare one-time webhook secret
        shell: bash
        run: |
          WEBHOOK_SECRET="$(openssl rand -hex 32)"
          echo "::add-mask::$WEBHOOK_SECRET"
          echo "WEBHOOK_SECRET=$WEBHOOK_SECRET" >> "$GITHUB_ENV"

      - name: Restrict Telegram identity
        shell: bash
        run: |
          printf '%s' "$TELEGRAM_CHAT_ID" | npx wrangler pages secret put TELEGRAM_ALLOWED_CHAT_IDS --project-name aperion-istasyon
          printf '%s' "$WEBHOOK_SECRET" | npx wrangler pages secret put TELEGRAM_WEBHOOK_SECRET --project-name aperion-istasyon

      - name: Deploy hardened Pages functions
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy . --project-name=aperion-istasyon --branch=main --commit-dirty=true

      - name: Register hardened Telegram webhook
        shell: bash
        run: |
          response="$(curl --fail --silent --show-error \
            --request POST \
            --data-urlencode "url=https://aperion-istasyon.pages.dev/telegram/webhook" \
            --data-urlencode "secret_token=$WEBHOOK_SECRET" \
            "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/setWebhook")"
          printf '%s' "$response" | jq -e '.ok == true' >/dev/null

      - name: Verify live security status
        shell: bash
        run: |
          response="$(curl --fail --silent --show-error https://aperion-istasyon.pages.dev/telegram/webhook)"
          printf '%s' "$response" | jq -e '
            .ok == true and
            .mode == "mobile-command-center-v1" and
            .identity_guard_configured == true and
            .webhook_secret_configured == true
          ' >/dev/null
